#include <stdio.h>
#include <stdlib.h>
#include "headers/structs.h"
#include "headers/menus.h"

void main(){
    // job *jobHead = NULL;

    // jobHead = read_job();

    // job *jtemp = jobHead;
    // read_job(jtemp);
    // printf("\n\n");
    MainMenu();
    // create_operation(jtemp, jtemp->operationHead, )
}